# Fedora-Bash-configuration-y-aliases
Bash conf para Fedora 42/43

Primero, crear el directorio donde se almacenará el archivo bash_aliases

```bash
mkdir ~/.bashrc.d
```

Luego, poner el archivo bash_aliases dentro de ~/.bashrc.d 

```bash
cp bash_aliases ~/.bashrc.d/
```

Finalmente, recargar el archivo .bashrc

```bash
source ~/.bashrc
```




